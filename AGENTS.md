This is a POC app that uses multiagents to simulate a characters for an interview. it has a frontend and a backend. 

The app should be really simple, with no database perisistence just enough for a demo